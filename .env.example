# PostgreSQL (used by docker-compose for db and to build DATABASE_URL for web)
POSTGRES_DB=messaging
POSTGRES_USER=messaging
POSTGRES_PASSWORD=change-me-secure-password

# Django
SECRET_KEY=change-me-in-production-use-django-insecure-for-dev-only
DEBUG=false
ALLOWED_HOSTS=localhost,127.0.0.1
